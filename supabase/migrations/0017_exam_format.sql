-- ---------------------------------------------------------------------
-- 0017: формат экзамена — параметр (Фаза 7.1).
--
-- «TÖMER» — семейство экзаменов: у каждого центра свой барем, тайминг
-- и минимум по секции; плюс государственный TYS с плавающими порогами.
-- Студент выбирает СВОЙ экзамен на странице моков; движок мока и
-- вердикт настраиваются пресетом (src/lib/exam/format.ts — числа там).
-- Без check-списка: реестр пресетов живёт в коде, незнакомое значение
-- честно откатывается на tomer_generic.
-- ---------------------------------------------------------------------

alter table profiles add column if not exists exam_format text not null default 'tomer_generic';

comment on column profiles.exam_format is
  'Пресет формата экзамена (src/lib/exam/format.ts): tomer_generic | tys | tomer_sakarya | tomer_bayburt';
