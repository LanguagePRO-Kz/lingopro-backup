# --- Supabase (public, safe for the browser) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only! Never prefix with NEXT_PUBLIC_. New format: sb_secret_...
# (Dashboard → API Keys → Secret key; legacy service_role JWT also accepted.)
SUPABASE_SECRET_KEY=

# --- Text AI (server-only) ---
# Model routing lives in src/lib/ai/models.ts (bake-off 07.07.2026).
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
# Global override: force EVERY text task onto one provider/model.
# AI_PROVIDER=anthropic
# AI_MODEL=claude-sonnet-4-6

# --- Voice teacher (ElevenLabs Agents, server-only) ---
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=

# --- Budget guard: monthly AI spend cap for the whole product, USD ---
AI_MONTHLY_BUDGET_USD=

# --- Payments (placeholders until merchant accounts exist) ---
KASPI_MERCHANT_ID=
KASPI_API_KEY=
POLAR_ACCESS_TOKEN=
